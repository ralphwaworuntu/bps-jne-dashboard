# Full tree is tracked. Only exception: Next.js build cache cannot be hosted on GitHub
# (files over 100 MB and ~1.3 GB total exceed normal Git + free LFS quota). Regenerate: npm run dev / build.
frontend/.next/
